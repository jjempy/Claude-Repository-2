[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm run db:generate"]

[start]
cmd = "npx prisma migrate deploy && node dist/server.js"
